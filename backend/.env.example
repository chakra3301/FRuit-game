# Fruit Game Backend Environment Variables

# Server
PORT=3001
NODE_ENV=development

# Frontend URL for CORS
FRONTEND_URL=http://localhost:3000

# Database (PostgreSQL)
# Local (docker-compose): postgresql://fruitgame_user:fruitgame_password@localhost:5432/fruitgame
# Railway/production: Use the provided DATABASE_URL
DATABASE_URL=postgresql://fruitgame_user:fruitgame_password@localhost:5432/fruitgame

# Solana Configuration
SOLANA_RPC_URL=https://api.devnet.solana.com

# Fruit Token (update when token is live)
FRUIT_TOKEN_MINT=PLACEHOLDER_TOKEN_MINT_ADDRESS

# Treasury Wallet (where play fees and pack purchases are sent)
TREASURY_WALLET=PLACEHOLDER_TREASURY_WALLET

# Admin Key for protected endpoints (generate a random string)
ADMIN_KEY=your-secret-admin-key-here

# Optional: Metaplex Configuration for NFT minting
# METAPLEX_AUTHORITY_KEYPAIR=path/to/keypair.json
# NFT_COLLECTION_ADDRESS=your-collection-mint-address
